//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import ParylationDomain 
import UIKit

final class {{ module_info.name }}BuilderImpl {
    private let dependency: {{ module_info.name }}Dependency

    init(dependency: {{ module_info.name }}Dependency) {
        self.dependency = dependency
    }
}

// MARK: - {{ module_info.name }}Builder implementation

extension {{ module_info.name }}BuilderImpl: {{ module_info.name }}Builder { 
    func build() -> UIViewController {
        let view = {{ module_info.name }}View()
        let component = {{ module_info.name }}Component()

        let navigationScene = NavigationScene(navigationController: dependency.navigationController)
        let interactor = {{ module_info.name }}InteractorImpl()
        let router = {{ module_info.name }}RouterImpl(
            navigationScene: navigationScene
        )
        let viewModel = {{ module_info.name }}ViewModelImpl(
            interactor: interactor,
            router: router
        ) 
        view.viewModel = viewModel
        return view
    }
}